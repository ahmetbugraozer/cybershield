name: secure_check
description: CyberShield - Kişisel Siber Güvenlik Asistanı
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # HTTP & API
  dio: ^5.4.0
  
  # UI & Animations
  google_fonts: ^6.1.0
  iconsax: ^0.0.8
  flutter_animate: ^4.3.0
  lottie: ^2.7.0
  fl_chart: ^0.65.0
  
  # Security & Crypto
  crypto: ^3.0.3
  zxcvbn: ^1.0.0
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Environment & Utils
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
